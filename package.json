{
  "name": "itelecnews",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "scrape": "turbo run scrape"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "packageManager": "npm@11.12.1",
  "engines": {
    "node": ">=22"
  }
}
